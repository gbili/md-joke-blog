import { expect } from 'chai';
import DirFilesToJson from '../../src/config/DirFilesToJson';

describe(`DirFilesToJson`, function() {
  describe(`generate(onSuccess, onFail)`, function() {
    it('should call onSuccess with the destination file if successful', function() {
    });
    it('should call onFail with an error if any error occurs while reading or writing', function() {
    });
    it('should create json file if extension is json', function() {
    });
    it('should create js file if extension is js', function() {
    });
    describe(`generate's created file`, function() {
      it('should be includable as an array of strings', function() {
      });
    });
  });
});
